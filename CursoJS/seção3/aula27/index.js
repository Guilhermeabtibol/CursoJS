/* Operadore Ternaria
(condicao) ? 'Valor para verdadeiro' : 'Valor para falso'
*/

const pontuacaoUsuario = 1000;
const nivelUsuario = pontuacaoUsuario >= 1000 ? 'Usuario VIP' : 'Usuario nomral';

crossOriginIsolated.log(nivelUsuario);
/* const pontuacaoUsuario = 999;

if (pontuacaoUsuario >= 1000) {
    console.log('Esse usuario é VIP');
} else {
    console.log('Esse usuario nao é VIP');
} */